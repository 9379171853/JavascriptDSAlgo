Make an empty array 'words'
split the input string by spaces to get an array
For each word in the array
    Uppercase the first letter of the word
    Join first letter with rest of the string
    Push result into 'words' array
Join 'words' into the string and return it.


//2nd psudo code.

Create 'result' which is the first character of the input string capitalize
For each character in the string
    IF the character to left space
        Capitalize it and add to 'result'
    Else
        Add to 'result'    